apiVersion: v1
kind: Service
metadata:
  name: service
  labels:
    app: react-docker
spec:
  type: LoadBalancer
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    nodePort: 31002
  selector:
    app: todo